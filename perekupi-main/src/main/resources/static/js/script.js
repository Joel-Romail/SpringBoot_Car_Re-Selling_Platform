var refresh_button = document.querySelector('input[value="Refresh"]')
refresh_button.addEventListener('click', function(evt){
    evt.preventDefault()
    var year = document.getElementById('filter_year').value
    //window.alert(year)
})